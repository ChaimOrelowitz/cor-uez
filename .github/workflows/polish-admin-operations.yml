name: Polish UEZ admin operations
on:
  push:
    paths:
      - '.github/workflows/polish-admin-operations.yml'
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Apply sole prop formation label
        run: python .github/scripts/label_sole_prop_formation.py
      - name: Verify frontend
        run: |
          npm install --ignore-scripts
          npm run build
      - name: Commit changes
        run: |
          git config user.name "COR UEZ Bot"
          git config user.email "chaim@corsolutions.io"
          git add src/AdminPage.jsx
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Clarify sole prop formation status"
            git push
          fi
