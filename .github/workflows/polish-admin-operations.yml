name: Polish UEZ admin operations
on:
  push:
    paths:
      - '.github/workflows/polish-admin-operations.yml'
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Apply service intro feedback
        run: python .github/scripts/polish_service_intro_feedback.py
      - name: Verify frontend
        run: |
          npm install --ignore-scripts
          npm run build
      - name: Commit changes
        run: |
          git config user.name "COR UEZ Bot"
          git config user.email "chaim@corsolutions.io"
          git add src/App.jsx src/intakePolish.css
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Polish UEZ service intro feedback"
            git push
          fi
