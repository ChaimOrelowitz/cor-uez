name: Polish UEZ admin operations
on:
  push:
    paths:
      - '.github/workflows/polish-admin-operations.yml'
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Add service intro, admin title, and client credentials
        run: python .github/scripts/add_service_intro_admin_title_restore_creds.py
      - name: Verify frontend
        run: |
          npm install --ignore-scripts
          npm run build
      - name: Commit changes
        run: |
          git config user.name "COR UEZ Bot"
          git config user.email "chaim@corsolutions.io"
          git add src/App.jsx src/AdminPage.jsx src/intakePolish.css
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Add UEZ service intro and legacy owner title support"
            git push
          fi
